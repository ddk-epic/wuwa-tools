import { Card, CardContent } from "./ui/card";

function CharacterSelect() {
  return (
    <Card>
      <CardContent>Character Profile</CardContent>
    </Card>
  );
}

export default CharacterSelect;
